[project]
name = "astr201-sp26"
version = "0.1.0"
description = "ASTR 201 Course Website - Spring 2026"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pyyaml>=6.0"]

[tool.setuptools]
# This is not a Python package - just dependency management
packages = []
